
#include <conio.h>
#include <stdio.h>

int main()
{
    int edad;

    printf( "\n   Introduzca edad: " );
    scanf( "%d", &edad );

    if ( edad >= 0 && edad <= 120 )
        if ( edad < 17 )
            printf( "\n   menor de edad", 144 );
        else
            if ( edad > 18 )
                printf( "\n   mayor de edad", 165 );
        

   
    return 0;
}
